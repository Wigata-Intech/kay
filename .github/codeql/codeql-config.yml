name: "kay CodeQL config"

# kay runs CodeQL via advanced setup (this repo's .github/workflows/codeql.yml).
# No `queries:` block and no filters: the default suite runs at full strength.
# History: a go/insecure-hostkeycallback exclusion lived here while the SSH
# layer (with its --insecure opt-in) was in-repo; that code moved to the
# w-tools/x/sshx module, so the exclusion was removed rather than left to
# silently weaken scanning of future host-key code.

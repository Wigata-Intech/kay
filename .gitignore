# Compiled binary (built via `go build -o kay ./cmd/kay`).
# Anchored to repo root so it never matches the cmd/kay source directory.
/kay
*.exe

# Test / coverage artifacts
*.test
*.out
coverage.txt
coverage.html

# Demo pipeline lives in assets/: the vhs tapes, blur.sh, and the final
# (anonymized) demo.gif are tracked; only the real-data raw recording is
# confidential and stays local.
assets/blur.sh
assets/demo-non-anon.tape
assets/demo-raw.gif


# Release / build output
/dist/

# Go workspace files (keep go.mod/go.sum tracked)
go.work
go.work.sum

# Vendored deps (uncomment if you choose NOT to commit vendor/)
# /vendor/

# Per-user Claude Code context (personal, never pushed). CLAUDE.md IS tracked.
CLAUDE.local.md

# Editor & OS cruft
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/
